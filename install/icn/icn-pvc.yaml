apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: icn-pvc
  namespace: filenet
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 2Gi